# Fix-DirectoryStructure.ps1
$ErrorActionPreference = 'Stop'
$rootPath = $PWD.Path

Write-Host "Fixing directory structure in $rootPath..." -ForegroundColor Cyan
Write-Host "Directory structure fixes complete." -ForegroundColor Green
